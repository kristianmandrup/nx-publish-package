# nx-publish-package

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build nx-publish-package` to build the library.

## Running unit tests

Run `nx test nx-publish-package` to execute the unit tests via [Jest](https://jestjs.io).
